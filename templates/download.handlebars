<div class="container">
    <h1 class='text-center my-5'>READ ME Generator</h1>
    <div class="card mb-5">
        <div class="card-header">
            <h4 class="text-center">Read Me Output File</h4>
        </div>
        <div class="card-body">
            <pre class="user-select-all" id="mdPart">{{mdFileText}}</pre>
            <a class="download-link btn btn-success btn-lg mx-auto" id="downloadButton">Download</a>
        </div>
    </div>
</div>